import { FilterUnknownPipe } from './filter-unknown.pipe';

describe('FilterUnknownPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUnknownPipe();
    expect(pipe).toBeTruthy();
  });
});
